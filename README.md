# Fall 2026 Deep Learning

Create the course environment with one command from the repository root:

```bash
python setup_environment.py
```

The setup selects PyTorch in this order: NVIDIA CUDA, Apple MPS, then CPU. It
creates `.venv` and registers the **Python (Fall 2026 Deep Learning)** Jupyter
kernel.
